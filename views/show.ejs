<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/CSS/app.css"/>
    <title>
      <%= category=== 'recommended'?'Highly Recommended': category ==='watch'?'Most Watched':'Enter If You Dare' %></title>
  </head>
  <body>
    <h1>
      <%= category === 'recommended'?'Highly Recommended': category ==='watch'?'Most Watched':'Enter If You Dare' %></h1>
       
      <% for (let i=0; i < movies.length; i++) {%>
      <img class="movie-image" src="<%= movies[i].image%>" alt="<%= movies[i].name%>">
      <%}%>
      <h2>Write Your Review</h2>
    
    <form action="/review" method="POST">
      Name: <input type="text" name="name" /> <br />
      Review: <input type="text" name="review" /> <br />
      <input type="hidden" name="category" value="<%= category %>" />
      <input type="submit" name="" value="Submit Review" />
    </form>
    
    <% for (let i = 0; i < reviews.length; i++){ %>
        <p><%=reviews[i].name%></p>
        <p><%=reviews[i].review%></p> 
        <nav> 
            <a href="/review/<%=reviews[i].id%>/edit">Edit</a>
        </nav>
        <form action="/review/<%=reviews[i].id%>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE"/>
        </form>
    <% } %>

    <nav>
      <a href="/">Back to Main Menu</a>
    </nav>
  </body>
</html>
